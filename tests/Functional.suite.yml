actor: FunctionalTester
modules:
    enabled:
        - Laravel:
            environment_file: .env.testing
            cleanup: true
        - Asserts
        - Db:
            dsn: 'mysql:host=mariadb;dbname=laravel_test'
            user: 'sail'
            password: 'password'
            cleanup: false
